name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: web/package-lock.json
      - name: install
        working-directory: web
        run: npm ci
      - name: typecheck (astro check)
        working-directory: web
        run: npm run check

  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: validate atoms manifest
        run: |
          # TODO: replace echo with real invocation once atoms-tools binary ships.
          # Wire: atoms validate ATOMS.yml
          # Tracking: convergent-systems-co/schema-atoms#121
          echo 'atoms validate: pending atoms-tools binary release'

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: web/package-lock.json
      - name: install
        working-directory: web
        run: npm ci
      - name: build
        working-directory: web
        run: npm run build
      - name: upload dist
        uses: actions/upload-artifact@v4
        with:
          name: site-dist
          path: web/dist/
          retention-days: 7
